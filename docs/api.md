# API Reference

> Auto-generated via [API Extractor](https://api-extractor.com/). Run `pnpm docs:generate` to regenerate.
> Last generated: 2026-07-21
> API report: `docs/connect-core.api.md`

---

## Table of Contents 
- [API Reference](#api-reference)
  - [Table of Contents](#table-of-contents)
  - [📄 API Report](#-api-report)
  - [📦 Package Overview](#-package-overview)
    - [`@naculus/connect-core`](#naculusconnect-core)
    - [`@naculus/connector-walletconnect`](#naculusconnector-walletconnect)
    - [`@naculus/connector-evm-injected`](#naculusconnector-evm-injected)
    - [`@naculus/connector-embedded`](#naculusconnector-embedded)
    - [`@naculus/connector-xrpl`](#naculusconnector-xrpl)
  - [🧪 Test Coverage](#-test-coverage)
  - [Quick Start (pure library)](#quick-start-pure-library)


## 📄 API Report

The full API surface is documented in [`docs/connect-core.api.md`](./connect-core.api.md) — auto-generated by `pnpm docs:generate`.

Covers all exports from `@naculus/connect-core`, `@naculus/connector-walletconnect`, `@naculus/connector-evm-injected`, etc.

---

## 📦 Package Overview

### `@naculus/connect-core`

Core abstractions and utilities:

| Export | Description |
|--------|------------|
| `createStorageAdapter()` | Factory: localStorage/sessionStorage/memory |
| `createEmptySession()` | Create a UniversalWalletSession with default values |
| `WalletError` | Typed error class with codes (user_rejected, wallet_unavailable, session_expired, etc.) |
| `createConnectorManager()` | Multi-connector lifecycle management |
| `createAutoReconnectManager()` | Session persistence and auto-reconnect |
| `setLogLevel()` | Logger level (debug/info/warn/error) |

**Types:** `UniversalWalletSession`, `UniversalConnector`, `ConnectorSupport`, `StorageAdapter`, `WalletErrorCode`, `Namespace`, `SessionNamespace`

> `@naculus/connect-appkit-react` and `@naculus/connect-appkit-ui` are documented in the [connect-appkit](../connect-appkit/) repo.

### `@naculus/connector-walletconnect`

WalletConnect v2 connector (`WalletConnectConnector`):

- `startPairing()` / `completePairing()` — dual-phase QR flow
- `connect()` / `reconnect()` / `disconnect()`
- `signMessage()`, `signTransaction()`, `sendTransaction()`
- `switchChain()` — EVM chain switching via `wallet_switchEthereumChain`
- `deepLink()` — mobile deep link redirection
- Supports eip155 + solana namespaces

### `@naculus/connector-evm-injected`

EIP-6963 multi-wallet discovery (`EIP6963ConnectorImpl` / `eip6963Connector` singleton):

- `startDiscovery()` / `stopDiscovery()` — browser event-based discovery
- `getDiscoveredWallets()` — list injected wallets (MetaMask, Coinbase, etc.)
- `connect()` — connect to first or specified wallet
- `onUpdate()` — subscribe to wallet discovery changes
- `getWalletByRDNS()` — lookup by rdns

**Utils:** `getEIP6963Provider()`, `isMetaMaskInstalled()`, `isCoinbaseWalletInstalled()`, `isWalletInstalled()`

### `@naculus/connector-embedded`

Self-custodial embedded wallet (`PocketConnector` / `createPocketConnector`):

- `createPocketConnector(config)` — factory, returns a configured PocketConnector instance
- `generateWallet()` — BIP39 mnemonic → HD key → EVM address
- `importFromMnemonic()` — import from 12/24-word phrase
- `importFromPrivateKey()` — import from raw 64-char hex
- `save()` / `load()` / `clear()` / `wipe()` — localStorage persistence (base64-encoded)
- `signMessage()` — `personal_sign` with secp256k1
- `sendTransaction()` — simulated tx with deterministic hash
- `switchChain()` — update chain ID

**Types:** `EmbeddedWalletConfig`, `WalletGenerationResult`, `EmbeddedWalletData`

### `@naculus/connector-xrpl`

XRP Ledger / Xaman connector (`XRPLConnectorImpl` / `xrplConnector` singleton):

- `connect()` — Xaman deep link with WebSocket message listener
- `disconnect()` / `getAccounts()`
- `signTransaction()` / `sendTransaction()` — Xaman deep link tx signing
- `switchChain()` — mainnet/testnet/devnet
- `createPaymentTx()` / `createTrustlineTx()` — helper builder methods
- `setNetwork()`, `getNetworkEndpoint()`

**Utils:** `createXRPLConnector()`, `formatXRPAmount()`, `parseXRPAmount()`, `isValidXRPAddress()`, `isValidXRPClassicAddress()`

---

## 🧪 Test Coverage

- Packages: core, walletconnect, evm-injected, xrpl, embedded, wallet-engine, siwx, connector-solana, connector-passkeys
- Framework: Vitest (node environment)
- Run: `pnpm test:run` or `pnpm test:coverage`
- React/UI tests live in [connect-react](https://github.com/naculus-official/connect-react)

---

## Quick Start (pure library)

```ts
import { ConnectorManager } from "@naculus/connect-core";
import { createWalletConnectConnector } from "@naculus/connector-walletconnect";
import { eip6963Connector } from "@naculus/connector-evm-injected";

const manager = new ConnectorManager();
manager.register("walletconnect", createWalletConnectConnector({
  projectId: "your-project-id",
  metadata: { name: "My App", description: "...", url: "...", icons: [] },
}));
manager.register("eip6963", eip6963Connector);

const session = await manager.connect("walletconnect");
```

For React quick start, see [connect-react docs](https://github.com/naculus-official/connect-react).

---

*Maintained by Naculus — regenerate with `pnpm docs:generate`*
